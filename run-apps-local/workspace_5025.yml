# ####################################################
# # Integrate a local running Microfrontend into OneCX
# ####################################################
# # Microfrontend: "workspace-ui"
# http:
#   routers:
#     local_workspace-ui:
#       # path prefix as used for MFE registration in product store
#       rule: "Host(`onecx.localhost`) && PathPrefix(`/mfe/workspace/`)"
#       service: local_workspace-ui
#       # higher prio than calculated based on rule length (traefik standard)
#       priority: 323
#       entryPoints:
#         - web
#       middlewares:
#         - strip_workspace-ui

#   # Remove the used mfe path so that it maps directly to localhost:port
#   middlewares:
#     strip_workspace-ui:
#       stripPrefix:
#         prefixes:
#           - "/mfe/workspace/"

#   services:
#     local_workspace-ui:
#       loadBalancer:
#         servers:
#           - url: "http://host.docker.internal:5025/" # Angular project started outside Docker with this port


# Run npm run start:local-env -- --port=5025 --public-host=http://localhost:5025